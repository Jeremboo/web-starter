
const assets = {};

export default assets;

/**
 * [loadAssets description]
 * @type {Promise}
 */
export const loadAssets = new Promise((resolve, reject) => {
  // TODO load all assets before resolving
  resolve();
});
