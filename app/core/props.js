const props = {
  ROTATION_SPEED: 0.01,

  debug: {
    helper: false,
    // webgl
    disableWebgl: false,
    orbitControlsMainCamera: true,
    postProcess: {
      disabled: false,
    },
  },
};

export default props;
