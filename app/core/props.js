const props = {
  ROTATION_SPEED: 0.01,

  debug: {
    postProcess: {
      disabled: false,
    },
    webglHelper: false,
    disableWebgl: false,
    orbitControlsMainCamera: false,
  },
};

export default props;
