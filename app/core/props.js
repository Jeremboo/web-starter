const props = {
  ROTATION_SPEED: 0.05,

  debug: {
    postProcess: {
      disabled: false,
    },
    orbitControlsMainCamera: true,
    webglHelper: false,
    disableWebgl: false,
  },
};

export default props;
