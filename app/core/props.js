const props = {
  ROTATION_SPEED: 0.01,

  debug: {
    postProcess: {
      disabled: false,
    },
    orbitControlsMainCamera: false,
    webglHelper: false,
    disableWebgl: false,
  },
};

export default props;
